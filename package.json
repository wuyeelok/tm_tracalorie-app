{
  "name": "tm_tracalorie-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 11846",
    "build": "vite build",
    "preview": "vite preview --port 16191",
    "build-netlify": "vite build --base=/",
    "preview-netlify": "vite preview --base=/ --port 16192"
  },
  "devDependencies": {
    "sass": "^1.72.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
